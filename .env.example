# Whisper transcription (required for live audio/video pipeline)
OPENAI_API_KEY=sk-your-key-here

# Gemini generation + RAG contextualization (required for live post generation)
GEMINI_API_KEY=your-gemini-key-here

# Parallel worker pool size for concurrent jobs
WORKER_COUNT=4

# Demo mode works without keys (sample transcript + template posts)
